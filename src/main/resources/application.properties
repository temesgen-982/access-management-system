spring.application.name=demo
# Reduce root logging noise (Options: ERROR, WARN, INFO, DEBUG, TRACE)
logging.level.root=WARN

# Keep your own application package cleanly visible at INFO level
logging.level.com.example.demo=INFO

# Optional: Keep Spring Security/Web specific logs clean when we get there
logging.level.org.springframework.web=WARN
